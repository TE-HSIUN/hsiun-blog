import { initNavigation } from '/js/navigation.js';
import { initMotion } from '/js/motion.js';

// Independent enhancements; the static HTML remains fully readable without JS.
initNavigation();
initMotion();
